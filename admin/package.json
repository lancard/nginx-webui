{
  "dependencies": {
    "admin-lte": "^4.0.0-rc3",
    "bootstrap": "^5.3.7",
    "bootstrap-icons": "^1.13.1",
    "chart.js": "^4.5.0",
    "chartjs-adapter-dayjs-4": "^1.0.4",
    "dayjs": "^1.11.13",
    "express": "^5.1.0",
    "express-session": "^1.18.2",
    "jquery": "^3.7.1",
    "nginxbeautify": "^2.0.1",
    "session-file-store": "^1.5.0",
    "systeminformation": "^5.27.7"
  },
  "name": "nginx-webui",
  "version": "1.3.1",
  "type": "module",
  "description": "nginx webui",
  "main": "src/index.js",
  "scripts": {
    "build": "npx esbuild index.js --bundle --format=esm --target=esnext --outdir=static/dist --minify --sourcemap --loader:.css=css --loader:.woff=file --loader:.woff2=file --loader:.ttf=file",
    "devServer": "node --watch server.js",
    "watch": "npx esbuild --watch index.js --bundle --format=esm --target=esnext --outdir=static/dist --minify --sourcemap --loader:.css=css --loader:.woff=file --loader:.woff2=file --loader:.ttf=file"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lancard/nginx-webui.git"
  },
  "keywords": [
    "nginx",
    "webui",
    "nginx-webui"
  ],
  "author": "Sungho Kim",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/lancard/nginx-webui/issues"
  },
  "homepage": "https://github.com/lancard/nginx-webui#readme",
  "devDependencies": {
    "esbuild": "^0.25.7",
    "esbuild-css-modules-plugin": "^3.1.5"
  }
}
